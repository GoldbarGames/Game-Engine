#include "InputManager.h"

// TODO: Why can't games find this file? 
// It should be linking when we build the game engine.
// No idea what is going wrong here,
// so the functions are in the header file instead.